{if $tree[$id][0] != ""}{foreach from=$tree[$id] key="key" item="subtree"}{if $subtree->id}<option value="{$subtree->id}"{if $char_category_id == $subtree->id} selected="selected"{/if}>{'&nbsp;'|str_repeat:($level*$offset)}{$subtree->name|stripslashes}</option>{/if}{if $level < $limit}{include file="tree_list.tpl" id=$subtree->id tree=$tree char_category_id=$char_category_id tree_url=$tree_url level=$level+1 offset=$offset limit=$limit}{/if}{/foreach}{/if}